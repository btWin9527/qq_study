{
  "compilerOptions": {
    "target": "es5",
    // 置顶ECMAScript 目标版本：'ES%'
    "module": "commonjs",
    // 指定使用模板：'commonjs','amd','system','umd' or 'es2015'
    "experimentalDecorators": true,
    // 启用实验性ES装饰器
    "allowSyntheticDefaultImports": true,
    // 允许从没有设置
    "sourceMap": true,
    // 把ts文件编译成js文件时，同时生成对应的map文件
    "strict": true,
    // 启用所有严格类型检查选项
    "noImplicitAny": true,
    // 在表达式和声明上有隐含的any类型时会报错
    "alwaysStrict": true,
    // 以严格模式检查模块, 并在每个文件里加入 'use strict'
    "declaration": true,
    // 生成对应的.d.ts文件
    "removeComments": true,
    // 删除编译后的所有注释
    "noImplicitReturns": true,
    // 不是函数的所有返回路径都有返回值时报错
    "importHelpers": true,
    // 从tslib导入辅助工具函数
    "lib": [
      "es6",
      "dom"
    ],
    // 指定要包含在编译中的库文件
    "typeRoots": [
      "node_modules/@types"
    ],
    "outDir": "./dist",
    "rootDir": "./src"
  },
  // 需要编译的ts文件一个*表示文件匹配**表示忽略文件的深度问题
  "include": [
    "./src/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "**/*.test.ts"
  ]
}
